[project]
name = "mcp-obs"
version = "1.0.0"
description = "Observability MCP server for nanobot"
requires-python = ">=3.11"
dependencies = ["mcp>=1.0.0", "httpx>=0.27.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
